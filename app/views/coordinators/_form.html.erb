<%= form_for(@coordinator) do |f| %>
  <% if @coordinator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coordinator.errors.count, "error") %> prohibited this coordinator from being saved:</h2>

      <ul>
      <% @coordinator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
     <%= f.label :course %><br>
     <%= f.collection_select :course_id, Course.all, :id, :name, :prompt => true %>
  </div>
  <div class="field">
    <%= f.label :employee %><br>
    <%= f.collection_select :employee_id, User.all, :id, :name, :prompt => true %>
  </div>
    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

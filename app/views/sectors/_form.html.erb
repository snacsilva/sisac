<%= form_for(@sector) do |f| %>
  <% if @sector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sector.errors.count, "error") %> prohibited this setor from being saved:</h2>

      <ul>
      <% @sector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group row">
    <%= f.label :name, "Nome", :class => 'col-sm-2 form-control-label' %>
    <div class="col-sm-10">
        <%= f.text_field :name, :class => 'form-control', :placeholder =>"Nome"%>
    </div>
</div>

<div class="form-group row">
    <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit 'Salvar', :class => 'btn btn-primary' %>
        <%= link_to "Cancelar", sectors_path, :class => 'btn btn-primary' %>
    </div>
</div>
<% end %>
